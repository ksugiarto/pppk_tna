<%= form_for(@request_participant) do |f| %>
  <% if @request_participant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@request_participant.errors.count, "error") %> prohibited this request_participant from being saved:</h2>

      <ul>
      <% @request_participant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :school_request_id %><br />
    <%= f.number_field :school_request_id %>
  </div>
  <div class="field">
    <%= f.label :teacher_id %><br />
    <%= f.number_field :teacher_id %>
  </div>
  <div class="field">
    <%= f.label :notes %><br />
    <%= f.text_area :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
