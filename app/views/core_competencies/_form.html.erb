<%= form_for([@curicculum, @vocational, @core_competency], :html => { :class => "form-horizontal" }, :remote => true) do |f| %>
  <% if @core_competency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@core_competency.errors.count, "error") %> prohibited this core_competency from being saved:</h2>

      <ul>
      <% @core_competency.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :seq_num, "#{t 'core_competency.seq_num'}", :class => "col-sm-4 control-label" %>
    <div class="col-sm-6"><%= f.text_field :seq_num, :required => true, :class => "form-control" %></div>
  </div>
  <div class="form-group">
    <%= f.label :description, "#{t 'core_competency.description'}", :class => "col-sm-4 control-label" %>
    <div class="col-sm-6"><%= f.text_area :description, :rows => 6, :required => true, :autofocus => true, :class => "form-control" %></div>
  </div>
  <div class="form-group">
    <div class="col-sm-4"></div>
    <div class="col-sm-6"><%= f.submit "#{t 'submit'}", :class => "btn btn-success", :disable_with => "#{t 'disable_with'}" %></div>
  </div>
<% end %>
