<%= form_for(@request_vocational) do |f| %>
  <% if @request_vocational.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@request_vocational.errors.count, "error") %> prohibited this request_vocational from being saved:</h2>

      <ul>
      <% @request_vocational.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :school_request_id %><br />
    <%= f.number_field :school_request_id %>
  </div>
  <div class="field">
    <%= f.label :vocational_id %><br />
    <%= f.number_field :vocational_id %>
  </div>
  <div class="field">
    <%= f.label :notes %><br />
    <%= f.text_area :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
