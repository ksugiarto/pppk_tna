<%= form_for(@curicculum, :html => { :class => "form-horizontal" }) do |f| %>
  <% if @curicculum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@curicculum.errors.count, "error") %> prohibited this curicculum from being saved:</h2>

      <ul>
      <% @curicculum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, "#{t 'curicculum.name'}", :class => "col-sm-4 control-label" %>
    <div class="col-sm-6"><%= f.text_field :name, :required => true, :autofocus => true, :class => "form-control" %></div>
  </div>
  <div class="form-group">
    <%= f.label :description, "#{t 'curicculum.description'}", :class => "col-sm-4 control-label" %>
    <div class="col-sm-6"><%= f.text_area :description, :rows => 3, :class => "form-control" %></div>
  </div>
  <div class="form-group">
    <div class="col-sm-4"></div>
    <div class="col-sm-6"><%= f.submit "#{t 'submit'}", :class => "btn btn-success", :disable_with => "#{t 'disable_with'}" %></div>
  </div>
<% end %>
